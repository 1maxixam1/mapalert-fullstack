services:
  - type: web
    name: mapalert-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: CORS_ORIGIN
        value: https://TU-SITIO.netlify.app
databases:
  - name: mapalert-db
    plan: free
